<h2>Active Polls</h2>

<ul>
  <% @polls.each do |poll| %>
    <li>
      <a href='/polls/<%= poll.id %>'><%= poll.name %></a>
      Created by <%= poll.creator.name %> on <%= poll.date_created.asctime %>
    </li>
  <% end %>
</ul>
